# Unity scene and prefab files
*.unity filter=lfs diff=lfs merge=lfs -text

# Unity asset files
*.asset filter=lfs diff=lfs merge=lfs -text

# Text-based file types that should not be tracked by LFS
*.txt -text

# Exclude specific large files or directories (customize as needed)
LargeAssets/SomeBigFile.png filter=lfs diff=lfs merge=lfs -text
LargeAssets/AnotherBigFile.zip filter=lfs diff=lfs merge=lfs -text

# Images (e.g., PNG, JPG, JPEG)
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text

# Music (e.g., MP3, WAV)
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# Videos (e.g., MP4, AVI)
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.avi filter=lfs diff=lfs merge=lfs -text

# 3D Models (e.g., FBX, OBJ)
*.fbx filter=lfs diff=lfs merge=lfs -text
*.obj filter=lfs diff=lfs merge=lfs -text
